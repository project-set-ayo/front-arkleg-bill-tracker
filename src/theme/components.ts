import { Components } from "@mui/material/styles";

const components: Components = {
  MuiButton: {
    styleOverrides: {
      root: {
        borderRadius: 8,
        textTransform: "none",
      },
    },
  },
};

export default components;
